export const styles = {
  container: {
    width: 320,
    backgroundColor: '#red',
    borderRight: '1px solid #e5e7eb',
    padding: 24,
  } as React.CSSProperties,
  cardHeader: {
    paddingBottom: 8,
  } as React.CSSProperties,
  section: {
    marginBottom: 16,
  } as React.CSSProperties,
  label: {
    display: 'block',
    fontSize: 12,
    color: '#374151',
    marginBottom: 6,
  } as React.CSSProperties,
  searchWrapper: {
    position: 'relative',
  } as React.CSSProperties,
  searchIcon: {
    position: 'absolute',
    left: 12,
    top: 12,
    width: 16,
    height: 16,
    color: '#9ca3af',
    pointerEvents: 'none',
  } as React.CSSProperties,
  searchInput: {
    paddingLeft: 40,
  } as React.CSSProperties,
};
